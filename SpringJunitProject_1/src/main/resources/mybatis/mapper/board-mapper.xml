<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sist.web.mapper.BoardMapper">
	<!-- 
		findAll() Page
		TO_CHAR == DATE_FORMAT
	 -->
	<select id="boardListData" resultType="com.sist.web.vo.BoardVO">
		SELECT no, subject, name, DATE_FORMAT(regdate, '%Y-%m-%d') AS dbday, hit
		FROM board
		ORDER BY no DESC
		LIMIT #{start}, 10
	</select>
	
	<!-- 
		count()
	 -->
	<select id="boardTotalPage" resultType="int">
		SELECT CEIL(COUNT(*)/10.0) FROM board
	</select>
	
	<insert id="boardInsert">
		INSERT INTO board(name, subject, content, pwd)
		VALUES(#{name}, #{subject}, #{content}, #{pwd})
	</insert>
	
	<update id="boardHitIncrement">
		UPDATE board SET
		hit = hit+1
		WHERE no = #{no}
	</update>
	
	<select id="boardDetailData" resultType="com.sist.web.vo.BoardVO">
		SELECT no, subject, name, content, DATE_FORMAT(regdate, '%Y-%m-%d') AS dbday, hit
		FROM board
		WHERE no = #{no}
	</select>
</mapper>